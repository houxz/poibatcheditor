<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.emg.poibatcheditor.dao.BuiltupareaModelDao" >
  <select id="select" resultType="map" parameterType="map">
    SELECT
    	M.builtuparea_id,
    	M.confirm_uid,
    	M.manualcheck_uid,
    	M.updatetime,
    	M.confirm,
    	M.autocheck,
		M.manualcheck,
		M.optype,
		M.confirm_timestamp,
		M.autocheck_timestamp,
		M.manualcheck_timestamp,
		M.isdel,
		M.ver,
		M.owner
    FROM tb_builtuparea M
    <where>
    	1=1
    	<if test="code != null" >
	      AND <![CDATA[ ${code}]]>
	    </if>
    </where>
    <if test="limit != null and limit > 0" >
      LIMIT #{limit}
    </if>
    <if test="offset != null and offset > 0" >
      OFFSET #{offset}
    </if>
  </select>
  
  <select id="count" resultType="int" parameterType="map">
    SELECT
    	count(1)
    FROM tb_builtuparea M
    <where>
    	1=1
    	<if test="code != null" >
	      AND <![CDATA[ ${code}]]>
	    </if>
    </where>
  </select>
  
  <select id="selectByOwners" resultType="map" parameterType="map">
    SELECT
    	M.builtuparea_id,
    	M.confirm_uid,
    	M.manualcheck_uid,
    	M.updatetime,
    	M.confirm,
    	M.autocheck,
		M.manualcheck,
		M.optype,
		M.confirm_timestamp,
		M.autocheck_timestamp,
		M.manualcheck_timestamp,
		M.isdel,
		M.ver,
		M.owner
    FROM tb_builtuparea M
	WHERE
		(M.optype = 'submit' OR M.optype = 'ready')
		AND M.owner IN
		<foreach collection="owners" item="listItem" open="(" close=")" separator="," >
			#{listItem}
		</foreach>
	<if test="limit != null and limit > 0" >
      LIMIT #{limit}
    </if>
    <if test="offset != null and offset > 0" >
      OFFSET #{offset}
    </if>
  </select>
  
  <select id="countByOwners" resultType="int" parameterType="map">
    SELECT
		count(1)
	FROM tb_builtuparea M 
	WHERE
		(M.optype = 'submit' OR M.optype = 'ready')
		AND M.owner IN
		<foreach collection="owners" item="listItem" open="(" close=")" separator="," >
			#{listItem}
		</foreach>
  </select>
</mapper>